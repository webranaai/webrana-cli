[package]
name = "hello-plugin"
version = "1.0.0"
edition = "2021"
description = "Example Webrana plugin"

[lib]
crate-type = ["cdylib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
lto = true
opt-level = "s"
